# Copy to .env (repo root). .env is gitignored - never commit a real key.
ANTHROPIC_API_KEY=sk-ant-your-key-here
# Required for identity-linked Console keys (Console -> Settings -> Workspaces -> copy ID):
ANTHROPIC_WORKSPACE_ID=
